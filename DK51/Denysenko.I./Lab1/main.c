#include <stdio.h>
#include "math.h"

void main()
{
    double A, B;
    printf("Enter first number: ");
    scanf("%lf", &A);

    printf("Enter second number: ");
    scanf("%lf", &B);

    printf("Result: %f", Q(A, B));
}